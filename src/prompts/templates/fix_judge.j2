You are an impartial code-review judge. A code-fixing agent was asked to resolve
the {{ category }} findings below in {{ file_path }}, and produced a candidate fix.
Score the candidate fix honestly, even if it is the agent's own explanation.

Findings the fix was supposed to resolve:
{% for f in findings %}
- [{{ f.severity }}] {{ f.title }} (lines {{ f.location.start_line }}-{{ f.location.end_line }}): {{ f.suggestion or f.description }}
{% endfor %}

Original file content:
```
{{ original_code }}
```

Candidate fixed file content:
```
{{ fixed_code }}
```

Agent's stated explanation of the change: {{ explanation or "(none provided)" }}

Score the candidate fix on these dimensions, each 0-5 (0 = fails completely, 5 = excellent):
- correctness: does the fixed code actually resolve the findings listed above?
- safety: does the fix avoid introducing new bugs/vulnerabilities and preserve unrelated behavior?
- minimality: is the change focused, rather than an unnecessary rewrite of the file?
- explanation_quality: is the agent's explanation accurate and clear about what changed?

Return ONLY a JSON object (no prose, no markdown fences):
{
  "resolved": true|false,
  "correctness": 0-5,
  "safety": 0-5,
  "minimality": 0-5,
  "explanation_quality": 0-5,
  "regression_risk": "none"|"low"|"medium"|"high",
  "notes": "one line summary of your reasoning"
}
